name: moviedb
description: Simple Movie DB app

publish_to: 'none'

version: 0.9.0+1

environment:
  sdk: ">=2.17.6 <3.0.0"
  flutter: ^3.0.5

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.5

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.1

flutter:
  uses-material-design: true
